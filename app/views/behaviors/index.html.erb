<h1>Listing Behaviors</h1>

<table>
  <thead>
    <tr>
      <th>Behavior</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @behaviors.each do |behavior| %>
      <tr>
        <td><%= behavior.behavior %></td>
        <td><%= link_to 'View', behavior %></td>
        <td><%= link_to 'Edit', edit_behavior_path(behavior) %></td>
        <td><%= link_to 'Delete', behavior, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Behavior', new_behavior_path %>
